{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "jsdom": "^20.0.2",
    "nuxt": "3.0.0-rc.12",
    "nuxt-icon": "^0.1.7",
    "sass": "^1.55.0"
  },
  "dependencies": {
    "@emailjs/browser": "^3.9.1"
  }
}
